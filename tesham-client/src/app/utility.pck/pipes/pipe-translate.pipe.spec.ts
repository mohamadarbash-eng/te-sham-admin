import { PipeTranslatePipe } from './pipe-translate.pipe';

describe('PipeTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
